'use strict';
let arr = ['325', '4689', '7256', '555', '795', '335', '459'];

for (let i = 0; i < arr.length; i++) {
	if ( arr[i].startsWith('3') ) {
		console.log(arr[i]);
	} else if ( arr[i].startsWith('7') ) {
		console.log(arr[i]);
	}
}